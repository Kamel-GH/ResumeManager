# v0.4 — Suivi de stabilisation

Branche de travail : `v0.4-stabilisation`

Objectif : stabiliser la branche `v0.4` sans changement fonctionnel visible, avant d'ajouter de nouvelles fonctionnalités lourdes.

## Règles de stabilisation

- Ne pas modifier le comportement utilisateur sans décision explicite.
- Préserver le modèle canonique `TemplateSchema -> RenderTree -> Renderer`.
- Ne pas mélanger refactor structurel et changement fonctionnel dans le même lot.
- Garder des commits courts, lisibles et réversibles.
- Valider localement après chaque lot avec `pnpm typecheck`, `pnpm test`, `pnpm build`.

## Lot 1 — Selectors / vues dérivées

Statut : en cours.

Objectif : sortir progressivement du store les vues dérivées utilisées par l'interface.

Fichiers créés :

- `features/editor/selectors/editor-pages-view.ts`
- `features/editor/selectors/editor-layers-view.ts`
- `features/editor/selectors/editor-objects-view.ts`
- `features/editor/selectors/index.ts`

Fonctions ciblées :

- `deriveEditorPagesView`
- `deriveEditorLayersView`
- `deriveEditorDocumentLayersView`
- `deriveEditorObjectsView`
- `filterEditorLayersView`
- `filterEditorObjectsView`

Décision : les nouveaux selectors sont créés dans un dossier dédié. Le prochain passage doit rebrancher les imports de l'interface vers `features/editor/selectors`, puis supprimer les exports équivalents du store lorsque le typecheck local confirme l'absence de régression.

## Lot 6 — Suivi des éléments obsolètes, partiels ou non branchés

Statut : initialisé.

| Élément | Statut | Risque | Décision proposée |
|---|---|---:|---|
| `EditorPanelId` / `openPanels` | probable héritage de l'ancien modèle de panneaux | Moyen | Vérifier les usages, puis supprimer si non utilisé |
| `assets` vs `libraries` | vocabulaire concurrent | Faible | Uniformiser progressivement sur `libraries` si c'est le terme produit retenu |
| `group` dans `RenderNodeType` | prévu mais non aligné avec `TemplateElementType` | Moyen | Décider : supprimer temporairement ou implémenter le groupage complet |
| `props.layerName` / `props.layerOrder` | duplication du modèle calque | Élevé | À traiter dans le Lot 5 avec migration éventuelle |
| Bouton `Paramètres` du volet gauche | action non évidente | Faible | Brancher un menu réel ou masquer temporairement |
| Copier/coller style Rich Text | actions visibles mais désactivées | Faible | Masquer tant que non branché ou implémenter proprement |
| Bibliothèques images / icônes / emoji / blocs texte | partiel ou statique | Moyen | Documenter comme partiel, puis brancher une vraie source de données |
| Gros fichier `editor-left-panel.tsx` | trop de responsabilités | Moyen | Lot 2 : découper en sous-composants |
| Gros fichier `konva-canvas-renderer.tsx` | trop de responsabilités | Élevé | Lot 3 : extraire hooks interactionnels |
| Gros fichier `rich-text-editor-panel.tsx` | toolbar + édition + variables + tables | Moyen | Lot 4 : extraire toolbar / variables / table controls |

## Checklist de validation manuelle après Lot 1

- Le volet gauche s'affiche correctement.
- Les onglets Pages / Calques / Objets fonctionnent.
- Les filtres Pages / Calques / Objets fonctionnent.
- La sélection d'une page change bien la page active.
- La sélection d'un calque ne casse pas la sélection des objets.
- La sélection d'un objet depuis le panneau Objets active la bonne page et sélectionne l'objet.
- La vue hiérarchie des calques reste fonctionnelle.
- La vue liste des calques reste fonctionnelle.

## Commandes de validation locale

```bash
pnpm typecheck
pnpm test
pnpm build
pnpm dev
```
